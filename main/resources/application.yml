server:
  port: 8081
  compression:
    enabled: true
spring:
  application:
    name: eureka-provider
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://admin:admin@localhost:1111/eureka